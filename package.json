{
  "name": "local-basket-css",
  "version": "1.0.0",
  "description": "CSS development tools for Local Basket application",
  "scripts": {
    "lint:css": "stylelint 'static/css/**/*.css' --fix",
    "format:css": "prettier --write 'static/css/**/*.css'",
    "build:css": "python build_css.py",
    "dev:css": "chokidar 'static/css/**/*.css' -c 'npm run build:css'",
    "test:css": "npm run lint:css && npm run build:css",
    "analyze:css": "python analyze_css.py",
    "validate:accessibility": "pa11y-ci --sitemap http://localhost:5000/sitemap.xml || echo 'Run flask app first'",
    "precommit": "npm run lint:css && npm run build:css"
  },
  "devDependencies": {
    "stylelint": "^15.10.3",
    "stylelint-config-standard": "^34.0.0",
    "stylelint-config-recess-order": "^4.3.0",
    "stylelint-order": "^6.0.3",
    "stylelint-selector-bem-pattern": "^3.0.1",
    "prettier": "^3.0.3",
    "chokidar-cli": "^3.0.0",
    "pa11y-ci": "^3.0.1"
  },
  "keywords": [
    "css",
    "design-system",
    "accessibility",
    "performance"
  ],
  "author": "Local Basket Team",
  "license": "MIT"
}